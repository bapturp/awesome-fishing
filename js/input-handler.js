export default class InputHandler {
    constructor(canvas, ctx) {
        this.canvas = canvas;
        this.ctx = ctx;
    };

    getEvents() {
        document.addEventListener('keydown', event => {
            switch (event.key) {
                case 'ArrowLeft':

            }
        })
    }

};
